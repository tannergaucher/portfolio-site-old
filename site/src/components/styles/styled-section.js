import styled from "styled-components"

const StyledSection = styled.div`
  margin: 10rem 0;

  .section-title {
    margin-bottom: 3rem;
  }

  span {
    margin-right: 1rem;
  }
`
export default StyledSection
