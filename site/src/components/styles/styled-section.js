import styled from "styled-components"

const StyledSection = styled.div`
  margin: 10rem 0;

  .section-title {
    margin-bottom: 4rem;
    font-weight: 900;
  }

  span {
    margin-right: 1rem;
  }
`
export default StyledSection
